version: '3.9'

services:

  db:
    image: postgres:16.2
    container_name: postgres_test_db
    restart: always
    ports:
      - "5432:5432"
    volumes:
      - ./database/despensa_postgres/postgres_data_pro:/var/lib/postgresql/data
    environment:
      POSTGRES_PASSWORD: despensa_pass